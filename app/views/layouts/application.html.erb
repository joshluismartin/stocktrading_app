<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "StockBit" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gradient-to-br from-green-50 to-green-100 min-h-screen">
    <!-- Modern Navbar for General Navigation -->
    <nav class="bg-gradient-to-r from-green-700 to-green-800 text-white px-6 py-4 shadow-lg border-b border-green-600">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-6">
          <div class="flex items-center space-x-2">
            <div class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center">
              <span class="text-white font-bold text-sm">$</span>
            </div>
            <span class="font-bold text-xl tracking-tight">StockBit</span>
          </div>
          <!-- General Navigation -->
          <%= link_to "Dashboard", root_path, class: "hover:bg-green-600 px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md" %>
          <%= link_to "Browse Stocks", stocks_path, class: "hover:bg-green-600 px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md" %>
          <% if user_signed_in? && current_user.admin? %>
            <%= link_to "Admin Panel", admin_traders_path, class: "bg-yellow-500 hover:bg-yellow-600 px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md font-medium" %>
          <% end %>
        </div>
        
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <div class="flex items-center space-x-3">
              <span class="bg-green-100 text-green-900 px-3 py-1 rounded-full text-sm font-medium">
                Welcome, <%= current_user.name.present? ? current_user.name : current_user.email %>!
              </span>
              <span class="bg-green-100 text-green-900 px-3 py-1 rounded-full text-sm font-medium">
                💰 <%= number_to_currency(current_user.balance) %>
              </span>
              <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md" %>
            </div>
          <% else %>
            <div class="flex items-center space-x-2">
              <%= link_to "Sign In", new_user_session_path, class: "bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg transition-all duration-200 hover:shadow-md" %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="flex">
      <!-- Sidebar for User-Specific Navigation -->
      <% if user_signed_in? %>
        <aside class="w-64 bg-white shadow-xl border-r border-green-200 min-h-screen">
          <div class="p-6">
            <h3 class="text-lg font-semibold text-green-800 mb-4 flex items-center">
              <span class="w-2 h-2 bg-green-500 rounded-full mr-2"></span>
              My Trading
            </h3>
            <nav class="space-y-2">
              <%= link_to portfolio_path, class: "flex items-center px-4 py-3 text-green-700 hover:bg-green-50 rounded-lg transition-all duration-200 hover:shadow-sm group" do %>
                <span class="mr-3 text-green-600">📊</span>
                <span class="group-hover:translate-x-1 transition-transform duration-200">My Portfolio</span>
              <% end %>
              
              <%= link_to trades_path, class: "flex items-center px-4 py-3 text-green-700 hover:bg-green-50 rounded-lg transition-all duration-200 hover:shadow-sm group" do %>
                <span class="mr-3 text-green-600">💹</span>
                <span class="group-hover:translate-x-1 transition-transform duration-200">My Trades</span>
              <% end %>
            </nav>
            
            <!-- Quick Stats -->
            <div class="mt-8 p-4 bg-gradient-to-r from-green-50 to-green-100 rounded-lg border border-green-200">
              <h4 class="text-sm font-semibold text-green-800 mb-2">Quick Stats</h4>
              <div class="space-y-1 text-sm text-green-700">
                <div class="flex justify-between">
                  <span>Balance:</span>
                  <span class="font-medium"><%= number_to_currency(current_user.balance) %></span>
                </div>
                <div class="flex justify-between">
                  <span>Status:</span>
                  <span class="font-medium capitalize"><%= current_user.status %></span>
                </div>
              </div>
            </div>
          </div>
        </aside>
      <% end %>
      
      <!-- Main Content Area -->
      <main class="<%= user_signed_in? ? 'flex-1 p-8' : 'w-full p-8' %>">
        <!-- Flash Messages with Enhanced Styling -->
        <% flash.each do |type, message| %>
          <div class="mb-6 p-4 rounded-lg shadow-md animate-fade-in
            <%= type == 'notice' ? 'bg-green-100 text-green-800 border border-green-300' : 'bg-red-100 text-red-800 border border-red-300' %>">
            <div class="flex items-center">
              <span class="mr-2"><%= type == 'notice' ? '✅' : '❌' %></span>
              <%= message %>
            </div>
          </div>
        <% end %>
        
        <%= yield %>
      </main>
    </div>
  </body>
</html>
